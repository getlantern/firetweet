<?xml version="1.0" encoding="UTF-8"?><!-- This is the background for the 
	dashed line -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
	<item>
		<shape>
			<solid android:color="@color/twimightBackgroundOwn" />
		</shape>
	</item>

	<!-- This is the bottom line -->
	<item>
		<shape>
			<solid android:color="@color/twimightBackgroundOwn" />
			<stroke android:width="4dp" android:color="@color/twimightSeparator"
				android:dashWidth="10dp" android:dashGap="7dp" />
		</shape>
	</item>

	<!-- This is the main color -->
	<item android:bottom="4dp">
		<shape>
			<solid android:color="@color/twimightBackgroundOwn" />
		</shape>
	</item>

</layer-list>